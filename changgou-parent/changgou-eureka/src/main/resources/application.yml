server:
  port: 6868
eureka:
  client:
    registry-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/
spring:
  application:
    name: eureka